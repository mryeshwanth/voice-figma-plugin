{
  "name": "figma-voice-to-text",
  "version": "1.0.0",
  "description": "A Figma plugin that converts voice to text using browser speech recognition",
  "scripts": {
    "build": "mkdir -p public && cp -r plugin/* public/ && cp -r web public/",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "keywords": [
    "figma",
    "plugin",
    "voice",
    "speech",
    "recognition",
    "transcription"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@vercel/kv": "^0.2.3",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mryeshwanth/voice-figma-plugin"
  }
}